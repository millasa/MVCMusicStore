<!DOCTYPE html>
<html>
    <head>
        <title>@ViewBag.Title</title>
        <link href="@Url.Content("~/Content/Site.css")"
    rel="stylesheet" type="text/css" />
        <script src="@Url.Content("~/Scripts/jquery-1.7.1.min.js")"
    type="text/javascript"></script>
    </head>
    <body>
        <div id="header">
            <h1>
                ASP.NET MVC MUSIC STORE</h1>
            <ul id="navlist">
                <li class="first">@Html.ActionLink("Home", "Index", "Home")</li>
                <li>@Html.ActionLink("Genres", "Index", "Store")</li>
                <li>@Html.ActionLink("Albums by Genre", "Browse", "Store")</li>
                <li>@Html.ActionLink("Album Details", "Details", "Store")</li>
            </ul>
        </div>
        @RenderBody()
    </body>
</html>

                